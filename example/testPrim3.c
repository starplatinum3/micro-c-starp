void main(int n)
{
     int res;
    //   Prim2 (">", Access (AccVar "n"), Prim3 (CstI 5, CstI 1, CstI 0)))));
    //  res=n > 5 ? 1 : 0;
    // 括号有用吗
      res=(n > 5) ? 1 : 0;
    //  他要返回一个值的 貌似不是stmt ，而是表达式吧。。？
    //  print res;
     print (res);
    //  print
    // print n > 5 ? 1 : 0;
//   1
// _1 CstI 5 但是也是拿到这个数字啊
}
